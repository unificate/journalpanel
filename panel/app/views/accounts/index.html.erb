<%= javascript_pack_tag 'accounts.js', 'data-turbolinks-track': 'reload' %>
<div align="center" class="user-container">
  <div class="mdc-data-table">
    <table class="mdc-data-table__table" aria-label="Management">
      <thead>
      <tr class="mdc-data-table__header-row">
        <th class="mdc-data-table__header-cell" role="columnheader" scope="col">Users</th>
        <th class="mdc-data-table__header-cell" role="columnheader" scope="col">Role</th>
      </tr>
      </thead>

      <% @users.each do |user| %>
        <tbody>
        <tr class="mdc-data-table__row">
          <td class="mdc-data-table__cell"><%= user.username %></td>
          <td class="mdc-data-table__cell">
            <div class="mdc-form-field">
              <%= radio_button_tag(user.username, "Admin", checked = user.is_role("admin")) %>
              <%= label_tag(user.username, "Admin") %>
              <div class="mdc-radio__ripple"></div>
            </div>
          </td>
          <td class="mdc-data-table__cell">
            <div class="mdc-form-field">
              <%= radio_button_tag(user.username, "Developer", checked = user.is_role("developer")) %>
              <%= label_tag(user.username, "Developer") %>
              <div class="mdc-radio__ripple"></div>
            </div>
          </td>
          <td class="mdc-data-table__cell">
            <div class="mdc-form-field">
              <%= radio_button_tag(user.username, "Product Owner", checked = user.is_role("product owner")) %>
              <%= label_tag(user.username, "Product Owner") %>
              <div class="mdc-radio__ripple"></div>
            </div>
          </td>
          <td class="mdc-data-table__cell">
            <div class="mdc-form-field">
              <%= radio_button_tag(user.username, "Stage Reviewer", checked = user.is_role("stage reviewer")) %>
              <%= label_tag(user.username, "Stage Reviewer") %>
              <div class="mdc-radio__ripple"></div>
            </div>
          </td>
        </tr>
        </tbody>
      <% end %>
    </table>
  </div>
</div>
