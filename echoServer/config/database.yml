default: &default
  adapter: mysql2 
  database: messageOfTheDay
  username: root 
  password: 123saymoo
  host: localhost

development:
  <<: *default
  database: db/messageOfTheDay

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/messageOfTheDay

production:
  <<: *default
  adapter: mysql2
  encoding: utf8
  # For actual production server, you will want to refer to an environment variable
  # on the server itself, not a value in this file.
  database: db/messageOfTheDay
